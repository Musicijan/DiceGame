import * as React from 'react';

const RollButton = () => {
  const roll = () => {}

  return (
    <div className="roll-btn">
      <button onClick={roll}>
        Roll
      </button>
    </div>
  )
}

export default RollButton;